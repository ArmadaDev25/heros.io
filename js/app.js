// DEBUG to make sure the JS file is linked to the HTML fileS
console.log('App is running and linked')

class GameLogic {
    renown = 0
    currency = 0
}

class Hero{
    constructor(name){
        this.name = name
    }
    
}